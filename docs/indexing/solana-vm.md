## `solana-vm` indexing

### Deposits

Deposits are indexed based on the `DepositEvent` emitted by the depository program, emitted after a successful `deposit_native` or `deposit_token` instruction execution. One event results in one deposit tracked by the oracle.

### Withdrawals

### Solver fills

### SOlver refunds
