# AGENTS.md

Guidance for AI agents working in this repository. Keep changes minimal and aligned with the existing code.

## Project

`relay-protocol-oracle` is the reference implementation of a Relay protocol oracle: a Dockerized Fastify HTTP service (TypeScript/Node) that attests on-chain events (e.g. depository deposits, solver fills/refunds) across multiple VMs and exposes a signed-attestation API. See [README.md](./README.md) for the operator quick-start and [docs/overview.md](./docs/overview.md) for what the oracle attests and its API surface.

## Setup

- Package manager: Yarn 1 (classic), pinned via `packageManager` in `package.json`.
- Install dependencies: `yarn install`.
- Runtime config comes from environment variables. Copy [.env.example](./.env.example) for a minimal local setup. Accessing RPC endpoints locally typically requires the VPN.
- `ENVIRONMENT` selects which `configs/chains.*.json` and `configs/hub.*.json` files load.

## Common commands

Verified against `package.json` scripts:

- Build: `yarn build` (`tsc -b`, emits to `dist/`)
- Run tests: `yarn test` (Jest)
- Start the service: `yarn start` (runs `dist/index.js`, so build first)
- Lint: `yarn lint` (ESLint, `--max-warnings 0`)
- Lint and autofix: `yarn lint:fix`
- Secret/SAST scan: `yarn semgrep`

Local dev convenience (from the README): `export $(cat .env | xargs) && yarn start` after creating `.env`.

## Layout

- `src/` — service source
  - `index.ts`, `http-server.ts`, `config.ts` — entrypoint, Fastify server, config loading
  - `api/` — HTTP routes and request handling
  - `services/attestation/` — attestation logic, including per-VM code under `services/attestation/vm/`
  - `signers/` — signing modules (`raw-private-key`, `aws-kms`)
  - `common/` — shared utilities (chains, signature verification, etc.)
- `test/` — Jest tests (`test/unit/`, `test/common/`)
- `configs/` — per-environment chain and hub config JSON
- `docs/` — `overview.md` and `indexing/` (per-VM indexing docs)
- `audits/`, `eslint-rules/`, `.semgrep/` — audits, custom lint rules, SAST config
- `Dockerfile`, `entrypoint.sh` — container build and startup

## Conventions

- Open a pull request for review; do not push directly to `main`.
- Match the style and structure of surrounding code; ESLint runs with `--max-warnings 0`, so keep lint clean.
- Do not bypass git hooks (no `git commit --no-verify`). The pre-commit hook runs `lint-staged` and a gitleaks secret scan; fix the underlying issue rather than skipping.
- Tests live under `test/`; add or update tests alongside changes.

<!-- BEGIN GENERATED SKILL INDEX -->
<!-- Generated by scripts/gen-configs.mjs — do not edit between these markers. -->

## Skill Index

Tool-neutral catalog of every skill in `skills/`. Read the linked file and follow its workflow.

| Skill | Description | Path |
|-------|-------------|------|
| analyze-video | Diagnose bugs from customer screen recordings attached to support conversations (Intercom, Pylon, Slack). | `skills/analyze-video/SKILL.md` |
| broadcast-breaking-change | Compose and prepare a Pylon broadcast for breaking changes, outages, or known issues affecting integrators. Use when: - A breaking API change is shipping that integrators need to act on - An active outage or degradation needs to be communicated to integrators - A known issue has been identified and integrators should be aware - A scheduled maintenance window needs advance notice - Someone says "broadcast", "notify integrators", "let integrators know", "send a broadcast", "draft a broadcast", "we need to tell integrators", "announce the outage", "announce the breaking change", "heads up to integrators", "pylon broadcast", "integrator comms" The output is a ready-to-paste broadcast for the Pylon UI (app.usepylon.com/broadcast). Requires: Datadog MCP (for outage context), Pylon MCP (for account/issue context), Slack MCP (for internal context). | `skills/broadcast-breaking-change/SKILL.md` |
| btc-investigation | Trace Bitcoin transactions, UTXOs, and ordinal inscriptions using Mempool.space and Hiro APIs. Use when a support ticket involves: - Tracing where BTC funds went after a swap or bridge - Investigating a lost/missing ordinal inscription - Checking if a BTC transaction is stuck or confirmed - Verifying UTXO spend status - Diagnosing fee-related issues on pending BTC transactions Bitcoin/BTC only — do NOT use for EVM transactions (use debug-relay-request or debug-tenderly-simulation) or Solana transactions (use debug-relay-request). Requires: WebFetch (for Mempool.space), Bash (for Hiro API curl calls). | `skills/btc-investigation/SKILL.md` |
| chain-health | Check the operational health of a specific blockchain chain on Relay. Use when someone asks "is X chain working?", "are we having issues with polygon/base/arbitrum?", "chain health", "chain status", "is there an outage on X?", or wants to know if Relay is experiencing issues on a specific chain. This checks a SINGLE chain's health. If the issue appears systemic (multiple chains, system-wide degradation, active incident response needed), escalate to on-call-incident-response. Do NOT use for debugging individual transactions — use debug-relay-request instead. | `skills/chain-health/SKILL.md` |
| compound-engineering | Compound Engineering workflow for AI-assisted feature development. Use when planning or implementing features, not for reviewing existing PRs (use compound-pr-review for that). Follows the Plan → Work → Review → Compound loop. Triggers on: plan this feature, implement this, create implementation plan, systematic development. | `skills/compound-engineering/SKILL.md` |
| compound-pr-review | Multi-agent PR review. Use when reviewing an existing PR, code diff, or asked to do a code review. Not for planning or implementing features (use compound-engineering for that). Runs parallel specialized reviewers and mines git history for patterns. | `skills/compound-pr-review/SKILL.md` |
| customer-response | Customer communication tone and empathy guidelines. Use when drafting any customer-facing response or when the user asks to write, draft, or compose a reply to a customer, integrator, user, or partner. Triggers on phrases like "write a reply", "draft a response", "reply to this customer", "respond to integrator", "message the user", or any request to compose outbound support communication. Also triggers on Intercom URLs (app.intercom.com/...), Pylon URLs (app.usepylon.com/...), or Slack context. Applies to support responses, technical explanations, bad news delivery, and follow-ups. | `skills/customer-response/SKILL.md` |
| datadog | Low-level Datadog MCP tool router. Use ONLY for direct data lookups (query metrics, search logs, list monitors). Do NOT use this for debugging transactions (use debug-relay-request), incident response (use on-call-incident-response), or chain health checks (use chain-health). This skill provides raw tool access, not workflows. | `skills/datadog/SKILL.md` |
| debug-integrator-issue | Debug an integrator support issue via Pylon. Use when: - A Pylon ticket URL is pasted - An integrator (wallet, payment processor, app) reports a technical issue - A diagnosis + technical response is needed for a developer audience The audience IS developers — be precise and technical. Entry point is always a Pylon ticket. For Intercom conversations, use debug-support-issue. For raw tx hashes without conversation context, use debug-relay-request. If deep on-chain trace analysis is needed for a failed fill, use debug-tenderly-simulation (EVM only). Requires: Pylon MCP, Datadog MCP, Relay Protocol MCP. | `skills/debug-integrator-issue/SKILL.md` |
| debug-relay-request | Investigate why a specific Relay request failed using Datadog logs and on-chain data. Use when: - A requestId, tx hash, or fill hash is provided and the question is "why did this fail?" - Someone asks "what happened with this transaction?", "what's happening with requestId X", "why did this request fail?", "debug this tx", "look into this request" - A request failed BEFORE a transaction was created — e.g., AMOUNT_TOO_LOW, UNSUPPORTED_ROUTE, fee calculation errors, quote failures, "can't initiate transaction" - A failed fill, reverted swap, or stuck request needs root cause analysis - The goal is UNDERSTANDING the failure — not refunding (use refund-prep), not replying to a user (use debug-support-issue or debug-integrator-issue) Produces a structured root cause analysis with: fault attribution, timeline, error chain, what went wrong, and (if applicable) suggested next steps (which may include triggering another skill). If the failure is an on-chain revert and you need to understand WHY the EVM call reverted, hand off to debug-tenderly-simulation for execution trace analysis. For BTC-specific tracing, use btc-investigation. After root cause is established and a refund is needed, use refund-prep. Requires: Datadog MCP. Optional: Relay Protocol MCP, block explorer lookups. | `skills/debug-relay-request/SKILL.md` |
| debug-support-issue | Debug an end-user support issue via Intercom. Use when: - An Intercom conversation URL is pasted - An end-user problem is described (stuck tx, missing funds, failed transfer) - A draft reply is needed for a non-technical user The audience is NOT developers — communicate simply and empathetically. Entry point is always an Intercom conversation. For Pylon tickets, use debug-integrator-issue. For raw tx hashes without conversation context, use debug-relay-request. If the user disputes what happened (wrong address, wrong amount), use posthog-frontend-investigator to verify their frontend session AFTER initial diagnosis. Requires: Intercom MCP, Datadog MCP, Relay Protocol MCP. | `skills/debug-support-issue/SKILL.md` |
| debug-tenderly-simulation | Debug a failed on-chain transaction using Tenderly. Use when: - Someone has a transaction hash and wants to know why it failed/reverted on-chain - A user reports a sell/deposit/fill error and you have the tx hash - Someone pastes a Tenderly shared simulation link (dashboard.tenderly.co/shared/simulation/...) - The `debug-relay-request` skill identified a failed fill tx and you need to understand WHY the on-chain call reverted - A failed fill needs deeper on-chain trace analysis beyond what Datadog logs provide EVM chains only — Tenderly does not support Solana, Bitcoin, or Tron. For BTC transaction tracing, use btc-investigation. For Solana/Tron, use block explorer lookups via debug-relay-request. Produces a structured root cause analysis from the Tenderly execution trace. Requires: WebFetch (no API key needed — works for both on-chain txs and shared simulations). | `skills/debug-tenderly-simulation/SKILL.md` |
| docs-improve | Identify documentation improvements from support conversations. Use when a support ticket, Intercom conversation, Pylon issue, or Slack thread reveals unclear, misleading, or missing documentation. Analyzes the customer's confusion, cross-references actual docs pages, and suggests specific improvements. | `skills/docs-improve/SKILL.md` |
| fellow-ai | Use when the user asks about meetings, meeting notes, meeting summaries, transcripts, action items, or Fellow.ai. Routes queries through the Fellow.ai MCP connector. | `skills/fellow-ai/SKILL.md` |
| flipside | Use when the user asks about blockchain data, crypto analytics, SQL queries on blockchain data, on-chain data, DeFi metrics, token analytics, or Flipside. Routes queries through the Flipside MCP connector. | `skills/flipside/SKILL.md` |
| generate-dashboard | Scaffold a personal role-based dashboard by cloning the personal-dashboard-template repo and configuring it for the user's role. Use when the user says "make me a dashboard", "generate a dashboard", "set up a dashboard for me", "give me a cockpit", or similar. Produces a local Next.js app tailored to their role (COO, head of support, BD lead, eng manager, etc.) with seed data they can edit. | `skills/generate-dashboard/SKILL.md` |
| git-conventions-compliance | Roll out Relay's git conventions to a repository. Use when asked to bring a repo to compliance, set up git conventions, run the bootstrap script, or configure PR templates and CI linting. Handles bootstrap script execution, scope taxonomy proposals, squash-merge configuration, and compliance verification. | `skills/git-conventions-compliance/SKILL.md` |
| health-check | Audit context files, rules, and skills for staleness, inconsistencies, and gaps. Use when the user says "health check", "audit context", "check for drift", or invokes /health-check. Also useful as a periodic maintenance task. | `skills/health-check/SKILL.md` |
| incident-report | Generate a customer-facing incident report after a RESOLVED production incident. Use when the user says "incident report", "write an incident report", "create a post-mortem for [customer]", "draft incident summary", or provides a Slack thread / Pylon issue / Intercom conversation / Linear issue about a resolved production incident that needs a formal write-up for the affected partner or customer. This is for RESOLVED incidents only — the incident must be over. For ACTIVE incident response (things are still broken), use on-call-incident-response instead. For checking chain status, use chain-health. | `skills/incident-report/SKILL.md` |
| intercom | Low-level Intercom MCP tool router. Triggers on Intercom URLs (app.intercom.com/...) and Intercom conversation IDs. Use ONLY for direct data lookups (search conversations, get contact, fetch ticket). Do NOT use this for debugging issues (use debug-support-issue), investigating tickets (use investigate-ticket), or drafting replies (use customer-response + intercom-format). This skill provides raw tool access, not workflows. | `skills/intercom/SKILL.md` |
| intercom-format | Intercom-specific formatting rules (HTML structure, character limits, visual layout). Auto-applies alongside customer-response whenever the reply destination is Intercom. Triggers on Intercom URLs (app.intercom.com/...), Intercom conversation data, or when drafting an Intercom reply. This skill handles HOW the message looks in Intercom. For WHAT to say and tone, see customer-response. | `skills/intercom-format/skill.md` |
| investigate-ticket | General-purpose ticket investigation and triage. Use when the user says "investigate", "look into", "check on", or "dig into" a ticket without specifying what action to take (refund, debug, reply). This skill reads the ticket, summarizes the situation, and recommends which specialized skill to use next (debug-support-issue, debug-integrator-issue, refund-prep, etc.). Triggers on Intercom URLs, Pylon issue numbers, or phrases like "investigate ticket", "check this ticket", "what happened with this customer". | `skills/investigate-ticket/SKILL.md` |
| legal-information-request | Process a legal or law enforcement information request. Use when: - A government agency requests transaction data (money laundering, theft investigation, subpoena) - A law enforcement request arrives via Pylon, Intercom, or Slack - Someone says "legal request", "legal information request", "government request", "law enforcement request", "legal investigation", or "information request" Takes a Pylon ticket URL, Intercom conversation URL, Slack thread link, relay.link transaction URL, list of addresses/tx hashes, or plain text. | `skills/legal-information-request/SKILL.md` |
| linear | Use when the user asks about Linear, issues, projects, initiatives, teams, cycles, milestones, documents, or project management. Routes queries through the Linear MCP connector. | `skills/linear/SKILL.md` |
| notion | Use when the user asks about Notion, pages, databases, documents, wikis, notes, knowledge base, or workspace content. Routes queries through the Notion MCP connector. | `skills/notion/SKILL.md` |
| on-call-incident-response | Guide ACTIVE incident response from detection through resolution. Use when: - An alert fires (Datadog monitor, PagerDuty, Slack alert) - Multiple users/integrators report the same issue simultaneously - System-wide degradation is suspected (not just one chain — use chain-health for single chain) - Someone says "incident", "on-call", "things are broken", "everything is down" Time-critical — move fast, mitigate first, investigate second. This is for ACTIVE incidents only. For writing up a RESOLVED incident, use incident-report instead. For checking if a single chain is healthy, use chain-health instead. Requires: Datadog MCP, Relay Protocol MCP, Slack MCP. | `skills/on-call-incident-response/SKILL.md` |
| posthog-frontend-investigator | Verify customer claims about transaction disputes using PostHog frontend session data. Use when: - A user DISPUTES what happened — claims wrong address, wrong amount, unauthorized transaction - You need to verify what the user actually did in our UI (clicked, confirmed, typed) - The question is "did the user actually do X?" not "why did the system fail?" Do NOT use when: - The issue is a system failure (stuck tx, failed fill, indexing delay) — use debug-support-issue - An Intercom URL is pasted for general debugging — use debug-support-issue first, then this skill only if the user's claim needs frontend verification - The issue is on the integrator/API side — use debug-integrator-issue This skill is a SECONDARY investigation tool — typically invoked after debug-support-issue identifies a claim that needs frontend verification. It reads PostHog session events (ADDRESS_MODAL_CONFIRMED, SWAP_CTA_CLICKED, etc.) to establish ground truth of user actions. | `skills/posthog-frontend-investigator/SKILL.md` |
| pr-review | Blocking, evidence-grounded PR review using gh CLI, before/after behavior tracing, and selective sub-agents. Use whenever the user asks to review a PR, diff, merge request, rebased branch, or AI-generated changeset. | `skills/pr-review/SKILL.md` |
| pylon | Low-level Pylon MCP tool router. Use ONLY for direct data lookups (search issues, get account, list contacts). Do NOT use this for debugging integrator issues (use debug-integrator-issue), investigating tickets (use investigate-ticket), or drafting replies (use customer-response). This skill provides raw tool access, not workflows. | `skills/pylon/SKILL.md` |
| qa-checklist | Generates comprehensive QA checklists from PR threads or diffs. Use when asked to create a QA checklist, test plan, or verification checklist for a PR. | `skills/qa-checklist/SKILL.md` |
| qa-queue | Fetch and prioritize the QA work queue from Linear. Use when the user says "qa queue", "what needs QA", "QA backlog", "/qa-queue", or wants to see tickets waiting on testing. | `skills/qa-queue/SKILL.md` |
| refund-prep | Prepare a ready-to-execute refund command for a failed or stuck transaction. Use when: - A refund ACTION is explicitly requested — "refund this", "process a refund", "prepare a refund", "can we refund this", "this user needs a refund" - A conversation link (Intercom or Pylon) is shared specifically for refund preparation - The root cause is ALREADY KNOWN and the decision to refund has been made Do NOT use when: - "Refund" is merely mentioned in a ticket but the issue hasn't been investigated yet — use debug-relay-request or debug-support-issue first to understand what happened - The goal is to understand WHY a transaction failed — use debug-relay-request instead - You're triaging a ticket and don't yet know if a refund is appropriate — use investigate-ticket Prerequisite: The transaction should already be investigated (via debug-relay-request or similar) before preparing a refund. Investigation first, refund prep second. Determines the correct recipient, amount, currency, and chain — then outputs a ready-to-execute refund command. Does NOT execute the refund — human executes. Requires: Datadog MCP, Relay Protocol MCP. Optional: Intercom MCP, Pylon MCP, Slack MCP. | `skills/refund-prep/SKILL.md` |
| report-compromised-wallet | Report a compromised wallet / drained funds incident. Use when: - A user reports their wallet was drained, hacked, or compromised - Unauthorized transactions are identified - A wallet needs to be blocklisted and reported to TRM and Chainpatrol - Someone says "compromised wallet report" followed by a link or description Takes an Intercom URL, Pylon URL, Slack link, or plain text description. Requires: Intercom MCP, Pylon MCP, Slack MCP, Datadog MCP, WebFetch. | `skills/report-compromised-wallet/SKILL.md` |
| review-api-key-request | Review an API key request and recommend approve/decline with Retool-ready payload. Use when someone asks to review an API key request, evaluate an integrator application, or provision a new API key. Triggers on phrases like "review this API key request", "provision API key", "evaluate this integrator", or when given a Google Form response row, Slack thread about a new integrator, or a company name requesting access. | `skills/review-api-key-request/SKILL.md` |
| running-test-suites | Runs solver test suites (unit, quote, swap execution, app fees, sponsorship) with isolated ports. Use when running tests locally or in parallel and needing separate env suffixes and service ports. | `skills/running-test-suites/SKILL.md` |
| skill-builder | Create new Claude Code skills. Use whenever the user wants to build, scaffold, or generate a skill — including when they say "turn this into a skill", "make a skill from this workflow", "save this as a skill", "create a slash command", or "add this to agents-config". Also invoke proactively when a conversation contains a useful repeatable workflow that the user might want to reuse. Don't wait to be asked explicitly — if the user just completed a multi-step investigation or workflow and there's a natural moment, suggest turning it into a skill. | `skills/skill-builder/SKILL.md` |
| slack | Low-level Slack MCP tool router. Use ONLY for direct data lookups (search messages, read channels, find users). Do NOT use this for incident response (use on-call-incident-response), broadcasting (use broadcast-breaking-change), or conversation mapping (use conversation-explainer). This skill provides raw tool access, not workflows. | `skills/slack/SKILL.md` |
| start-day | Morning startup routine. Use when the user says "start day", "morning briefing", "what did I miss", or invokes /start-day. Pulls repos, checks overnight activity, surfaces what needs attention. | `skills/start-day/SKILL.md` |
| update-dashboard | Refresh a personal dashboard's data by pulling from connected MCPs (Notion, Fellow.ai, Linear, Slack, Google Calendar). Writes to the dashboard's data/seed.json. Use when the user says "update dashboard", "refresh dashboard", "pull latest into dashboard", or as a step in start-day. Works on any dashboard created by the generate-dashboard skill. | `skills/update-dashboard/SKILL.md` |
| update-errors | Sync the Relay error message glossary from solver code + Notion and update local tools (relay-tx-investigator.html). Use when the user says "update errors", "sync error glossary", "refresh error codes", or invokes /update-errors. | `skills/update-errors/SKILL.md` |
| update-repos | Pull latest on core git repos in the workspace. Use when the user says "update repos", "pull repos", "sync repos", or invokes /update-repos. | `skills/update-repos/SKILL.md` |
| update-skills | Pull latest skills from agents-config and symlink any new ones. Use when the user says "update skills", "sync skills", "pull skills", "get new skills", or invokes /update-skills. | `skills/update-skills/SKILL.md` |
| visual-explainer | Generate a visual HTML explainer site from any source — conversations, concepts, docs, codebases, investigations, incidents, proposals, or any topic that benefits from a polished visual breakdown. Use when the user pastes a support ticket URL, Slack thread URL, conversation URL, docs page URL, or asks to "map", "visualize", "explain", or "create an explainer" for any topic. Also triggers on phrases like "map this", "create an explainer", "visualize this", "explain how X works", "break down this conversation". Use this skill even if the user just provides a URL or topic and wants a visual output — don't wait for them to say "explainer" explicitly. | `skills/visual-explainer/SKILL.md` |
| wrap-up | End-of-session wrap-up for project work. Updates session-state.md with what was done, decisions made, and what's next. Use when finishing a project session, or when the user says "wrap up", "end session", "save progress", or invokes /wrap-up. | `skills/wrap-up/SKILL.md` |

<!-- END GENERATED SKILL INDEX -->
